name: mcp_browser
description: Universal Web Automation Backbone — 4-Primitive Contract over headless browsers (CDP-direct), authenticated sessions, search, crawl, DOM extraction.
version: 0.1.0
homepage: https://app-appplayer.github.io/makemind
repository: https://github.com/app-appplayer/mcp_browser
issue_tracker: https://github.com/app-appplayer/mcp_browser/issues

topics:
  - browser
  - automation
  - mcp
  - cdp
  - crawl

environment:
  sdk: ^3.7.2

dependencies:
  mcp_bundle: ^0.3.0
  meta: ^1.15.0
  collection: ^1.19.1
  uuid: ^4.0.0
  crypto: ^3.0.3
  html: ^0.15.4
  args: ^2.4.0
  web_socket_channel: ^3.0.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
